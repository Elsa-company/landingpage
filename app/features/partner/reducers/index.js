import resetPartnerState from './resetPartnerState';
import skipReducer from './skip';

const reducers = {
	resetPartnerState : resetPartnerState,
	setSkip           : skipReducer,
};

export default reducers;
